# 🔐 Password Generator Tool
Create strong and secure passwords to keep your account safe online . 
## ⚙️ Features
- Generates strong, secure passwords
## 🚀 Usage
1. Make sure you're logged in as **root** (or use `sudo`).
2. Run the generator:

```bash
chmod 777 password_generate
sudo ./password_generate
